# Components

React components will go here:

- `Board.tsx` - Main game board with zones
- `Card.tsx` - Individual card component
- `Hand.tsx` - Player's hand display
- `HeroPanel.tsx` - Hero stats and abilities
- `ResourceBar.tsx` - Resource display
- `PileSelector.tsx` - Draw pile selection
